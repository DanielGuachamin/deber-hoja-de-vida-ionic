import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'deber-hoja-de-vida',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
